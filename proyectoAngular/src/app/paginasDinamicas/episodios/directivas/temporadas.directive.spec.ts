import { TemporadasDirective } from './temporadas.directive';

describe('TemporadasDirective', () => {
  it('should create an instance', () => {
    const directive = new TemporadasDirective();
    expect(directive).toBeTruthy();
  });
});
