import { FormatearFechaDirective } from './formatear-fecha.directive';

describe('FormatearFechaDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatearFechaDirective();
    expect(directive).toBeTruthy();
  });
});
