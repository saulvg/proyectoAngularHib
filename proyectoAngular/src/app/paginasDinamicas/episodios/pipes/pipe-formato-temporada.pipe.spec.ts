import { FormatoTemporadaPipe } from './pipe-formato-temporada.pipe';

describe('FormatoTemporadaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoTemporadaPipe();
    expect(pipe).toBeTruthy();
  });
});
