import { PipeFormatoFechaPipe } from './pipe-formato-fecha.pipe';

describe('PipeFormatoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFormatoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
