import { MostrarIconosGeneroDirective } from './mostrar-iconos-genero.directive';

describe('MostrarIconosGeneroDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarIconosGeneroDirective();
    expect(directive).toBeTruthy();
  });
});
