import { VivoMuertoDirective } from './mostrar-iconos-vivoMuerto.directive';

describe('MostrarIconosDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarIconosDirective();
    expect(directive).toBeTruthy();
  });
});
