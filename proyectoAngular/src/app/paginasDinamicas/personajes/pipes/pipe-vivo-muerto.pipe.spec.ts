import { PipeVivoMuertoPipe } from './pipe-vivo-muerto.pipe';

describe('PipeVivoMuertoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeVivoMuertoPipe();
    expect(pipe).toBeTruthy();
  });
});
