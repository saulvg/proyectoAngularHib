import { PipeDescripcionPipe } from './pipe-descripcion.pipe';

describe('PipeDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
