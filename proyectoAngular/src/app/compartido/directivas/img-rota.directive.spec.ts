import { ImgRotaDirective } from './img-rota.directive';

describe('ImgRotaDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgRotaDirective();
    expect(directive).toBeTruthy();
  });
});
