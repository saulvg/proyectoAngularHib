import { InfoExtraDirective } from './info-extra.directive';

describe('InfoExtraDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoExtraDirective();
    expect(directive).toBeTruthy();
  });
});
