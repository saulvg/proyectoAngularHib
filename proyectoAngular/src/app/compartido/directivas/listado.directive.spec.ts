import { ListadoDirective } from './listado.directive';

describe('ListadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ListadoDirective();
    expect(directive).toBeTruthy();
  });
});
