import { MostrarCrudDirective } from './mostrar-crud.directive';

describe('MostrarCrudDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarCrudDirective();
    expect(directive).toBeTruthy();
  });
});
